apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iletken.fullname" . }}-test-connection"
  labels:
    {{- include "iletken.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iletken.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
