apiVersion: v1
kind: Service
metadata:
  name: {{ include "iletken.fullname" . }}
  labels:
    {{- include "iletken.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iletken.selectorLabels" . | nindent 4 }}
